/** Local Sourdex service base URL. The service binds to loopback only (PRD §17.3). */
export const API_BASE = "http://127.0.0.1:8787";

/** Key for the paired Bearer token in chrome.storage.local. */
export const TOKEN_KEY = "sourdex:token";
